message = "Hello"

main proc()
  name = "world"
  age = 75
  greet(name, age)
end

greet proc(name: string, age: i32)
  printf("%s, %s!\n", message, name)
  printf("You are %d years old.\n", age)
end
