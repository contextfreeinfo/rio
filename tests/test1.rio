use "c"
use "./test1-other"
include "./test1-more"

do
  name = "world"
  age = 75
  # Need structs next for spans to work.
  scores = [45, 63, 22, -8]
  words = ["hi", "there"]
  for words do(word)
    printf("word: %s\n", word)
  end
  greet(name, age)
  greet_person({name, age = 80})
  # a.b().c.d
end

Person struct
  name: string
  age: i32
end
