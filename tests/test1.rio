use "c"
use "./test1-other"
include "./test1-more"

do
  name = "world"
  age = 75
  score = 4.5
  # Two kinds of spans, and two examples of one.
  # Try span of Person after fixing struct order.
  scores = [45.0, 63.1, 22.2, -8.3]
  mores = [1, -5]
  words = ["hi", "there"]
  # Loop with receiver.
  for words do(word)
    printf("word: %s\n", word)
  end
  # Loop alone.
  for words
    printf("hi\n")
  end
  # Call functions.
  greet(name, age)
  # Struct instance literal, with shorthand.
  show_person({name, age = 80, scores})
  # a.b().c.d
end

Person struct
  name: string
  age: int
  scores: [float]
end
