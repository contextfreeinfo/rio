use "c"
use "./test1-other"
include "./test1-more"

do
  name = "world"
  age = 75
  # Need structs next for spans to work.
  scores = [45, 63, 22, -8]
  mores = [1]
  words = ["hi", "there"]
  for words do(word)
    printf("word: %s\n", word)
  end
  for words
    printf("hi\n")
  end
  greet(name, age)
  greet_person({name, age = 80})
  # a.b().c.d
end

Person struct
  name: string
  age: i32
end
