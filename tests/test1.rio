use "c"
use "./test1-other"
include "./test1-more"

do
  name = "world"
  age = 75
  # Need structs next for spans to work.
  scores = [45, 63, 22, -8]
  # Then for loop.
  greet(name, age)
  greet_person({name, age = 80})
  # a.b().c.d
end

Person struct
  name: string
  age: i32
end
