message = "Hello"

bother fun(x: float): float
  x + 1
;

bother_again proc(x: float)
  condition = x + 1 > 2
  if condition
    printf("Something!\n")
  ;
;

has_child_ticket_price proc(age: int): bool
  if age < 18
    printf("You'd like to think that, wouldn't you?\n")
  ;
  age < 13
;

sum fun(n: int): int
  result var := 0
  result := result + n
  #~ for 1 ..= n do(i)
    #~ result := result + i
  #~ end
  result
;

ticket_price_calc proc(age: int): float
  if
    has_child_ticket_price(age)
      4.50
    ;
    else
      5.50
    ;
  ;
;
