message = "Hello"

bother fun(x: float): float
  x + 1
end

bother_again proc(x: float)
  # This should infer to bool.
  condition = x + 1 > 2
  if condition
    printf("Something!\n")
  end
end

has_child_ticket_price proc(age: int): bool
  if age < 18
    printf("You'd like to think that, wouldn't you?\n")
  end
  age < 13
end

sum fun(n: int): int
  result var := 0
  result := result + n
  #~ for 1..=n do(i)
    #~ result := result + i
  #~ end
  result
end
