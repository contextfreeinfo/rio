use "c" => ..

main = for () with printf be
  each (0.thru 9) for n be
    void of printf "fib(%d) = %d\n" n (fib n)
  end
end

fib pub = for n as int to int be
  case of
    n <= 0 be 0
    n == 1 be 1
    else be vouch of fib (n - 2) + fib (n - 1)
  end
end
