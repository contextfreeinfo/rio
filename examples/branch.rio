main* = for() be
    describe(2)
    # TODO Parse negatives.
    # describe(-1)
    describe(0)
end

describe = for(i: Int32) be
    # TODO Fix this parsing.
    # print description(n)
    print(description(i))
end

description = for(i: Int32) be
    message = branch with
        i < 0 to "negative"
        i > 0 to "positive"
        else "zero"
    end
    message
end
