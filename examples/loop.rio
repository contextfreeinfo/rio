# Log the message a given number of times.
logRepeatedly :: proc(message: String, times: Int)
  n := 0
  while n < times
    log(message)
    n += 1
  end
end

# Greet until we say bye.
main :: proc()
  logRepeatedly("Hi there!", 3)
  log("Bye now!")
end
