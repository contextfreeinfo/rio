# Log the message a given number of times.
loopLog :: proc(message: String, times: Int)
  n := 0
  for n < times
    log(message)
    n += 1
  end
end

# Greet until we say bye.
main :: proc()
  loopLog("Hi there!", 3)
  log("Bye now!")
end
