main* = for() be
    count-down(3)
end

count-down = for(count: Int32) be
    print("counting ...")
    branch with
        count > 1 to count-down(count - 1)
        else print("done")
    end
end

is-even = for(i: Int32) be
end
