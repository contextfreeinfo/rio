#!/usr/bin/env bash

if [[ "$1" == "-g" ]]; then
  run=valgrind
else
  run=""
fi

mkdir -p bin
# Use c++11 because it's nice but also old enough it has better support.
time c++ -Wall -Wpedantic -std=c++11 -o bin/rio src/main.cpp
time $run ./bin/rio -i tests/test1.rio
