#static_assert(sizeof(long) == 8)
#static_assert(sizeof(ulong) == 8)

@foreign
const LONG_MIN = INT64_MIN as long;

@foreign
const LONG_MAX = INT64_MAX as long;

const ULONG_MIN = INT64_MIN as ulong;

@foreign
const ULONG_MAX = INT64_MAX as ulong;
